apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: taskflow

spec:

  selector:
    app: backend

  ports:

    - port: 3000
      targetPort: 3000

  type: ClusterIP